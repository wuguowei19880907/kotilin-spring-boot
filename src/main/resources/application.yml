server:
  port: 8083
spring:
  profiles:
    active: @profileActive@

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace
